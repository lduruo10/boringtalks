<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.boringtalks.activities"
    android:versionCode="1"
    android:versionName="1.0" >

    <!-- Min version of android to run is 2.1 -->
    <uses-sdk android:minSdkVersion="7" />
    
    <!-- Set up the permissions that we need for app -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    

    <!-- Start app configuration sets -->
    <application
        android:icon="@drawable/launcher"
        android:label="@string/app_name" >
        
        <!-- The first activity, show a welcome image -->
        <activity
            android:name="com.boringtalks.activities.WelcomeActivity"
            android:label="@string/app_name" 
            android:theme="@android:style/Theme.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        
        <activity android:name="ConversationActivity"
            android:theme="@android:style/Theme.NoTitleBar"></activity>
        
        <activity android:name="AboutActivity"
            android:theme="@android:style/Theme.NoTitleBar"></activity>
        
   
        
        
        
    </application>

</manifest>